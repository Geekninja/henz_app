= simple_form_for(resource, as: resource_name, url: session_path(resource_name),  html: { :class => "form-signin" } ) do |f|
  %h2.form-signin-heading Gestão Henz
  .login-wrap
    .user-login-info
      = f.input :email, required: false, autofocus: true, placeholder: 'digite seu e-mail'
      = f.input :password, required: false, placeholder: '********'
    = f.button :submit, "Acessar", class: 'btn btn-lg btn-login btn-block'
    